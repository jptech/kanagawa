[package]
name = "kanagawa_ast"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Typed AST for Kanagawa language (lowered from CST)"

[dependencies]
# CST types for lowering
kanagawa_syntax = { path = "../kanagawa_syntax" }

# CST tree structure
rowan.workspace = true

# Utilities
thiserror.workspace = true

[dev-dependencies]
