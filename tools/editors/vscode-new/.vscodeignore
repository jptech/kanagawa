# Development files
.git/**
.vscode/**
.vscode-test/**
src/**
test/**

# Explicitly include production dependencies (negate gitignore patterns)
!node_modules/
!node_modules/web-tree-sitter/**

# Exclude all other node_modules except web-tree-sitter
node_modules/*
!node_modules/web-tree-sitter

# Compiled test files (not needed in production)
out/test/**

# Build artifacts (we use out/ for runtime)
**/*.ts
**/*.map
*.vsix
*.tgz

# Test configuration
.mocharc.json
tsconfig.test.json

# Documentation (internal)
# Exclude all markdown files except the normal included ones (README & CHANGELOG)
**/*.md
!README.md
!CHANGELOG.md

# Grammar source and build dependencies (only need compiled WASM in dist/)
grammar/**

# Dist folder JS files (we use out/ for JS, but dist/ for WASM)
dist/**/*.js
dist/**/*.js.map
dist/providers/**
dist/service/**

# ESLint config
.eslintrc.json

# Temp test folders
vsix-test/**
